---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { THEMES } from '../../../consts';
import { getNamesByTheme } from '../../../data/names';

const title = 'Kategori Nama Bayi Islam';
const description =
  'Teroka nama bayi Islam mengikut kategori: rezeki, rahmat, soleh, cahaya, cantik dan lagi.';
---

<BaseLayout title={title} description={description} canonicalPath="/nama-islam/makna/">
  <nav class="breadcrumbs" aria-label="Breadcrumb">
    <a href="/">Laman utama</a> / <a href="/nama-islam/">Nama Islam</a> / <span>Kategori Nama</span>
  </nav>
  <h1>{title}</h1>
  <p class="lead">{description}</p>
  <div class="card-grid">
    {Object.entries(THEMES).map(([slug, theme]) => {
      const count = getNamesByTheme(slug).length;
      return (
        <a class="card" href={`/nama-islam/makna/${slug}/`}>
          <h3>{theme.label}</h3>
          <p>{count} nama · {theme.blurb}</p>
        </a>
      );
    })}
  </div>
</BaseLayout>
