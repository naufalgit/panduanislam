---
import { EN_BASE, NAMA_BAYI_BASE } from '../consts';
import { genderLabel, genderLabelEn, type BabyName } from '../data/names';

interface Props {
  names: BabyName[];
  lang?: 'ms' | 'en';
}

const { names, lang = 'ms' } = Astro.props;
---

<ul class="name-list">
  {names.map((n) => (
    <li>
      <a href={lang === 'en' ? `${EN_BASE}/names/${n.slug}/` : `${NAMA_BAYI_BASE}/nama/${n.slug}/`}>
        <strong>{n.name}</strong>
        <span>
          {lang === 'en' ? genderLabelEn(n.gender) : genderLabel(n.gender)} ·{' '}
          {lang === 'en' ? n.meaningEn : n.meaning}
        </span>
      </a>
    </li>
  ))}
</ul>
